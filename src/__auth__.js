/**
 * 1. login === sing in ==> u are an old user. you already have the credential (email and pass) to login
 * 2. register === sing up === create account ===> you    are a brand new user. you have never used the website before you do not have any email and pass that have access
 */